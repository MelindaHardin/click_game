import React from "react";

const Header = () => (
  <div class="bootstrapJumbo">
    
    <div class="jumbotron jumbotron-fluid">
      <div class="container">
        <h1 class="display-4">Click Game!</h1>
        <p class="lead">Click on an image to earn points, but don't click the same image more than once!.</p>
      </div>
    </div>
    
  </div>
);

export default Header;